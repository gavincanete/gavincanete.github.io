// This file contains JavaScript code for interactive elements of the web portfolio.

document.addEventListener('DOMContentLoaded', () => {
    const profileImage = document.querySelector('.profile-image');
    const techStacksImage = document.querySelector('.tech-stacks-image');

    // Example of an interactive feature: Click event on profile image
    profileImage.addEventListener('click', () => {
        alert('Profile image clicked!');
    });

    // Additional functionality can be added here
});